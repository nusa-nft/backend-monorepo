pipeline {
  agent any
  stages("Build API") {
    stage {
      echo("build API")
    }
  }
  
  stages("Build Worker") {
    stage {
      echo("build Worker")
    }
  }

  stages("Build Indexer") {
    stage {
      echo("build Indexer")
    }
  }
  
  
}
